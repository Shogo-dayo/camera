# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'newCamera' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftyJSON' , '~> 4.0' 
  pod 'Alamofire' 


  # Pods for newCamera

  target 'newCameraTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'newCameraUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
